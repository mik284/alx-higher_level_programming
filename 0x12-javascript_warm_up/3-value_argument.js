#!/usr/bin/node

//Write a script that prints the first argument passed to it:

function testR(Args) {

    if (Args == "") {
        console.log("No argument");
    } else {
        console.log(Args);
    }

}